# TruthLens AI Backend

This backend exposes a Flask API for fake-news prediction.

## Run

```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

API: `http://localhost:5000/api/predict`
